import CategoriesTitle from "../CategoriesTitle";
import withTranslation from "../../../../withTranslation";
const Kitchens = ({ t }) => {
  return <CategoriesTitle name={t("t16")} />;
};

export default withTranslation(Kitchens);
