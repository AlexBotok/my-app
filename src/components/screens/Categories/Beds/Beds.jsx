import CategoriesTitle from "../CategoriesTitle";
import withTranslation from "../../../../withTranslation";
const Beds = ({ t }) => {
  return <CategoriesTitle name={t("t13")} />;
};

export default withTranslation(Beds);